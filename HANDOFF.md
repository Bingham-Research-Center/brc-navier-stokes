# Handoff: R3C terminal defect spectrum

**Updated:** 2026-07-25 · **Clay status:** unsolved

**Live route:** `ROUTE-R3C` · **Checkpoint:**
`EXP-TYPE-II-TERMINAL-DIMENSION-PINCER-001`

## Load only

- [Dimension/spectral theorem](dossier/experiments/type-ii-terminal-dimension-pincer.md)
- [`ROUTE-R3C`](dossier/records/routes.json)

## Exact q4 theorem chain

- Retained energy-efficient carriers are impossible; the survivor is diffuse.
- A future layer cannot assemble below
  \(\Lambda_j\asymp R_j^{-7/6}j^{2/3}\).
- Every Leray--Hopf continuation loses fixed kinetic energy at \(T^*\).
- The defect is supported on a terminal singular slice with
  \(3/5\le\dim_{\rm H}\sigma\le1\) and \(\mathcal H^1(\sigma)=0\).
- Direct Besov energy-equality interpolation fails in all four regions.
- Direct pressure interpolation has \(\gamma\ge8/11\); the
  Barker--Wang pincer-closing necessary range is \(\gamma<1/4\).

These are conditional repository theorems pending external review. They do
not prove a Clay alternative.

## Exact live question

Can one finite adjoint/square-function budget sum branch 1, or can branch 2
be forced to reside long enough to dissipate? At least one branch holds; if
branch 1 fails, branch 2 holds. They need not be mutually exclusive.

1. **Bounded clock band:** fixed defect energy stays below
   \(A\Lambda_j\). Disjoint intervals then carry fixed-sign nonlinear work
   against changing band tests; viscosity tends to zero. Available nonlinear
   capacity is exactly order one per event, so no common sum is known.
2. **Super-clock escape:** defect energy lies above
   \(A_j\Lambda_j\), \(A_j\to\infty\), forcing
   \(\|\nabla u(s_j)\|_2^2\gtrsim A_j^2\Lambda_j^2\). No residence is known.

## Next bounded cycle

Work only on branch 1: construct one piecewise transported/adjoint test that
bundles the fixed-sign correlations. Audit its strain, pressure, jump, and
terminal costs against the q4 tail. Stop and record the exact obstruction if
the cost is merely critical. Do not repeat norm interpolation, the
three-fifths optimisation, or frozen-band scalar correlations.

Keep branch 2, divergent normalised energy, clocks at or below the
five-power barrier, and \(2/5\le\gamma<1/2\) self-similarity separate.
